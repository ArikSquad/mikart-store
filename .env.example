TEBEX_PUBLIC_TOKEN=

# browser checkout URL override. the embedded checkout falls back to the
# basket's links.checkout URL when no checkout request ident is available
NEXT_PUBLIC_SITE_URL=http://localhost:3000
